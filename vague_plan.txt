 Gemini said
1. Schema Migrations

    sets table: Add distance (REAL, nullable) and duration_seconds (INTEGER, nullable). Modify weight and reps to be nullable.

    categories table: Add activity_type (TEXT, default 'strength').

2. Payload Parser (ui/prompt.cr)

    Contextual Branching: Pass the movement's activity_type to the parser.

    Strength Logic: Retain [weight] [reps] [rpe].

    Endurance Logic: Add [distance] [duration] [rpe].

        Parse distance as a float (miles or km based on preference).

        Parse duration strings (MM:SS or HH:MM:SS) into integer seconds.

3. CLI Logging Flow (commands/log.cr)

    Dynamic Prompts: Check activity_type in Phase 4. If 'endurance', output: Enter: distance duration rpe (e.g., 1.5 30:00 6).

    Brick Workouts: Leverage the existing Phase 5 "Sticky Loop". Selecting (c) Change movement inherently supports stacking swim, bike, and run efforts under a single daily session_id.

4. Reporting & Summaries (commands/summary.cr)

    Conditional Formatting:

        Strength sets render as: Set 1: 185.0 x 8 @ RPE 7.

        Endurance sets render as: Effort 1: 3.10 in 24:30 @ RPE 7 (Pace: 7:54/m).

    Volume Aggregation: Split the summary block to report total weight lifted alongside total distance/time accumulated.

    Status/Decay: Endurance movements will seamlessly reset "Last Hit" timers for mapped targets (e.g., Quadriceps, Cardio) via the existing V1 logic.

5. Seed Data Additions

    Categories: Add Swim, Bike, Run (all flagged as activity_type: 'endurance').

    Targets: Introduce Cardiovascular or Aerobic Base.

    Movements: Add basic entries like Freestyle Swim, Zone 2 Run, Tempo Run, Cycling. Map these to both the new cardio targets and existing leg targets.